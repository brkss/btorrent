package main

import (
	"fmt"
)

func main() {
	alo := make([]int, 10)
  wee := make([]int, 5);
  for i := 0; i < 10; i++ {
    alo[i] = i;
  }
  copy(wee[:], alo[4:10]);
  for i := 0; i < 5; i++ {
    fmt.Printf("wee[%d] = %d\n", i, wee[i]);
  }
  
}

